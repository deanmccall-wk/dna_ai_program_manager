name: 'dna_ai_program_manager'
version: '1.0.0'
config-version: 2

profile: 'user'

model-paths: ["models"]

models:
  dna_ai_program_manager:
    staging:
      +tags: ["bronze"]
      +materialized: incremental
    intermediate:
      +tags: ["silver"]
      +materialized: incremental
    marts:
      +tags: ["gold"]
      +materialized: table
